# 1.6b narrative-summary evidence

This records the real-path verification for the summarization half of 1.6b.
Relevance filtering remains separately deferred.

## Live BLOG simulation

Command:

```text
RUN_LIVE_NARRATIVE_SUMMARY=1 MONGODB_DATABASE=competitors_monitoring_test \
  python -u tests/live_narrative_summary_verification.py
```

The real Lyfe Marketing `/blog` simulation created `NEW_BLOG` change
`6aa0e373e0e027603f65949e`. The generated narrative was:

> A new blog post titled “Leveraging AI to Scale Social Media Management in 2026” has been added to the page, appearing after the existing content. It includes a thumbnail image, author/date meta, and structured content with sections on why AI matters, a practical playbook, common pitfalls, and a CTA to contact LYFE Marketing.

The mechanical summary remained:

```text
NEW_BLOG: 36 line(s) added, 1 line(s) removed (2851 characters added, 77 removed).
```

The counting provider recorded two text generations: one existing mutation
generation and one narrative generation.

## Product simulation exclusion

The same real run simulated a JD Sports product change and created
`NEW_PRODUCT` change `6aa0e389e0e027603f6594a0`:

```text
change_type: NEW_PRODUCT
narrative_summary: null
mechanical_summary: NEW_PRODUCT: HOKA Bondi 7 Mutated Edition (165.00) at /product/black-hoka-bondi-7-mutated-edition/19800000_jdsportsau
mutation JSON calls: 1
narrative text calls: 0
```

This confirms the feature made no additional LLM call for a product event.

## Forced failure

The live verifier then ran `monitor_target` against a real Lyfe target with a
provider whose narrative call raises `TimeoutError`. The run still completed
with status `SUCCESS` and created `NEW_BLOG` change `6aa0e38fe0e027603f6594a3`:

```text
narrative_summary: null
mechanical_summary: NEW_BLOG: 4 line(s) added, 1 line(s) removed (207 characters added, 77 removed).
failing provider calls: 1
```

## Real backfill

The backfill command initially selected 9 records and updated 8. One very
large single-line HTML diff returned an empty provider response; rerunning the
same command with the configured `gpt-4o` model updated that final record.
The idempotent verification rerun was:

```text
database=competitors_monitoring_test selected=0 updated=0 failed=0 dry_run=False
```

Before/after examples from the real `changes` collection:

| Change | Mechanical-only before | Narrative after |
| --- | --- | --- |
| `6a9b68092c48c07004cba192` | `NEW_BLOG: 1 line(s) added, 1 line(s) removed (70723 characters added, 70723 removed).` | The form wrapper’s browser-specific class changed from `gf_browser_chrome` to `gf_browser_unknown`. The hidden Akismet field’s value changed from `146` to `85`. |
| `6a9cf6be7a0878901d044d69` | `NEW_BLOG: 1 line(s) added, 1 line(s) removed (70724 characters added, 70723 removed).` | The honeypot field label in the form changed from `Email` to `Phone`. The hidden Akismet JS value changed from `85` to `219`. |

The final MongoDB aggregate over `NEW_BLOG` and `PAGE_UPDATE` reported
`total: 10, with_narrative: 10` in `competitors_monitoring_test`.

## Automated regression coverage

```text
135 passed, 5 skipped, 6 subtests passed in 19.46s
```

The suite includes creation-time narrative generation, product-event
exclusion, forced graceful failure, repository backfill updates, simulation
persistence wiring, API serialization, and the Django changes-feed display.
