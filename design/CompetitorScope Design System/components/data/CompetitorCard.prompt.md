One-line: the competitor summary card — the single bold element on the dashboard screen.

```jsx
<CompetitorCard name="Lyfe Marketing" website="lyfemarketing.com" activeTargets={3} recentChanges={12} onOpen={openDetail} />
```

The initial disc uses the brand tint, not a logo — no competitor logos exist in the source and none should be fabricated.
