One-line: shows a candidate's `page_type` plus how it was classified — plain type, never a colored badge.

```jsx
<PageTypeLabel type="PRODUCT_LISTING" method="LLM" />
```

Don't give page types their own color scale; the badge slot in a row belongs to status and change type.
