One-line: the white screen header — title, optional breadcrumb, right-aligned screen actions.

```jsx
<TopBar breadcrumb="Competitors" title="Lyfe Marketing" meta="lyfemarketing.com"
  actions={<><Button variant="secondary" icon="plus">Add manually</Button><Button icon="refresh-cw">Run Discovery</Button></>} />
```
